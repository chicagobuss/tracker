.env
.env.*
!.env.example
pgdata/
# compiled binary at repo root only (anchored, so skills/tracker/ isn't ignored)
/tracker
*.log
.serena/
backups/

# python bytecode
__pycache__/
*.pyc

# local data directory
data/
